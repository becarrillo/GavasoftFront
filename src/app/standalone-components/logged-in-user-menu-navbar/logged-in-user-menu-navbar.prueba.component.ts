/*import { ComponentFixture, TestBed } from '@angular/core/testing';

import { LoggedUserMenuNavbarComponent } from './logged-in-user-menu-navbar.component';

describe('LoggedUserMenuNavbarComponent', () => {
  let component: LoggedUserMenuNavbarComponent;
  let fixture: ComponentFixture<LoggedUserMenuNavbarComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      imports: [LoggedUserMenuNavbarComponent]
    })
    .compileComponents();
    
    fixture = TestBed.createComponent(LoggedUserMenuNavbarComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});*/
